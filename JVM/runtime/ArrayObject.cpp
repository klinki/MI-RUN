#include "ArrayObject.h"
/*
template <>
void ArrayObject<Object*>::accept(ObjectVisitorInterface * visitor)
{
	for (int i = 0; i < this->size; i++)
	{
		this->arrayData[i]->accept(visitor);
	}
}

template <>
void ArrayObject<Object*>::accept(ObjectVisitorInterface & visitor)
{
	this->accept(&visitor);
}
*/
